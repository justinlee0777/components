export default interface AnimateOptions {
  /** Number of milliseconds per frame. Default is ~~16.7ms for 60fps. */
  msPerFrame?: number;
  /** Duration in milliseconds of the scroll. Default is 300ms. */
  duration?: number;
}
